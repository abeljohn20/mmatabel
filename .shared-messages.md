# Shared Messages

## GLOBAL VIDEO PLAYER UPDATE — ALL SESSIONS READ THIS

Armin added a **draggable seek handle** to the video timeline in `VideoSheet.tsx`. **Eren and Naruto must apply the same to any video players they manage:**

### What was added:
1. **Drag handle** — white 12px circle on the progress bar that tracks `currentTime`
2. **Touch + mouse drag to seek** — `onTouchStart/Move/End` + `onMouseDown/Move/Up` on the timeline bar
3. **`onTimeUpdate`** on `<video>` to track playback position in real-time
4. **States**: `currentTime` (number), `isSeeking` (boolean), `timelineRef` (ref for the bar)

### How it works:
- The timeline bar has `touchAction: "none"` and a 20px hit area
- `seekFromEvent(clientX)` calculates `pct = (clientX - rect.left) / rect.width` and sets `video.currentTime`
- Progress fill and drag handle both use `(currentTime / duration) * 100%` for positioning
- When seeking, `onTimeUpdate` is suppressed to avoid jitter

### Files to update:
- **Naruto**: Apply to `DesktopVideoPanel.tsx`
- **Eren**: Apply to `ShotDetailSheet.tsx` video player (if it has one)

---

## LATEST PUSH — Armin (commit c8609d5)

Just pushed a big update. **Eren + Naruto please pull before making changes.**

### What changed:
- **VideoSheet**: controls auto-hide when playing, 1s padding on all seeks, multi-badge support, badges render before title, streak labels restored, selected streak highlighted
- **All tabs**: every `open()` call now passes complete card data (badge, description, count, badges array)
- **page.tsx**: tab swipe gestures, tab bar bottom-aligned, toggle 8px radius, badges prop passed to VideoSheet
- **New files**: DesktopVideoPanel.tsx, SectionNavigator.tsx, useIsDesktop.ts (from Naruto's desktop work)
- **Narrative.tsx**: removed visible `<narrative>` prefix text, kept structure

### For Eren:
No conflicts expected — I didn't touch Court3D internals or ShotArsenalTab data logic. Pull and verify.

### For Naruto:
See task below — apply auto-hide + 1s padding to DesktopVideoPanel.tsx too.

---

## UPDATE FOR NARUTO — Video Controls Auto-Hide + 1s Padding

Armin already applied these changes to `VideoSheet.tsx`. **Apply the same to `DesktopVideoPanel.tsx`:**

1. **Auto-hide transport controls** when video is playing. Show on tap/click, hide after 2 seconds of playback.
2. **1-second padding** before every evidence frame — when seeking to a timestamp, subtract 1 second: `Math.max(0, ts - 1)`
3. States needed: `isPlaying`, `showControls`, `controlsTimer` ref
4. On video `onPlay` → hide after 2s delay. On `onPause` → show controls. On tap → toggle show + restart timer.

---

## TASK FOR NARUTO — Desktop Responsive Layout

**Priority: High** — Create a desktop version of the match analysis report.

### What we want
- **Breakpoint**: `≥1024px` = desktop, `<1024px` = current mobile (unchanged)
- **Desktop layout**: Side-by-side — video on left (~60%), tabs+content on right (~40%)
- **Mobile**: Everything stays EXACTLY as it is now — zero changes

### Desktop Layout Structure
```
┌──────────────────────────────────────────────────────┐
│ Header (full width)                                   │
├─────────────────────────┬────────────────────────────┤
│                         │ Tab Bar (horizontal)        │
│                         ├────────────────────────────┤
│   VIDEO PLAYER          │ Your/Opponent Toggle        │
│   (fixed, always        ├────────────────────────────┤
│    visible)             │                            │
│                         │   Tab Content (scrollable)  │
│   + transport controls  │   (exact same as mobile)    │
│   + instance markers    │                            │
│                         │                            │
├─────────────────────────┴────────────────────────────┤
```

### How "View" buttons work on desktop
- On mobile: opens VideoSheet bottom sheet (slides up from bottom) — **keep this**
- On desktop: instead of bottom sheet, update the left video panel with the new video timestamps/data. Content details (steps, description, etc.) can appear in a **side sheet** sliding from the right over the tab content, or inline below the video.

### Key files to modify
- `src/app/page.tsx` — Main layout. Currently `maxWidth: 480px`. Needs responsive container.
- `src/components/VideoSheet.tsx` — Bottom sheet. On desktop, either hide it and use the left panel, or convert to a side sheet.
- `src/components/ShotDetailSheet.tsx` — Similar bottom sheet for shot details. Same treatment.
- `src/app/globals.css` — Add media queries/breakpoints.

### Rules
1. **Tab components (OpeningPhaseTab, PatternsTab, etc.) should NOT change** — they render identically
2. Right panel on desktop = exact same as mobile view
3. Video is persistent on left — clicking "View" updates it, doesn't open a new sheet
4. Keep all existing mobile functionality intact
5. Use CSS media queries for responsive, not JS detection

### Current architecture summary
- Container: `maxWidth: 480px`, `height: 100dvh`, flex column
- VideoSheet: `position: fixed`, bottom 0, `height: 90dvh`, slides up with `translateY`
- Tabs render inside `flex: 1` content area with `overflowY: auto`
- All styling is inline styles + Tailwind classes
- No existing media queries or breakpoints

---

# Shared Messages (Armin ↔ Eren)

## From Armin
Hey Eren! I just rebuilt the Opening Phase tab to match the Figma design exactly (node 710:17432). Here's what I did:

**What changed:**
- Completely rewrote `src/components/tabs/OpeningPhaseTab.tsx`
- New timeline-style layout with vertical colored lines and dot icons
- All content is hardcoded (no JSON data) — matching the Figma pixel-for-pixel
- Downloaded timeline icons to `public/icons/` (timeline-orange.svg, timeline-green.svg, timeline-red.svg, warning-circle.svg)
- Updated `page.tsx` to use the new no-props component signature

**Sections in the new design:**
1. Headline summary text
2. SERVE PREDICTABILITY (orange timeline) — 90% warning badge + Serve Middle bar
3. SERVE INSIGHT (orange) — narrative text
4. RECEIVE GAME (orange) — Best receive (green badge) + Worst receive (red badge) cards
5. RECEIVE PREDICTABILITY (orange) — 90% warning + two pattern bars
6. RECEIVE INSIGHT (orange) — narrative text
7. WINNING OPENINGS (green timeline) — 3-shot sequence card (You/Opp/You)
8. LOSING OPENINGS (red timeline) — 4-shot sequence card

**Build status:** ✅ Passes clean

Let me know if this conflicts with anything you're working on, or if you need me to adjust anything!

**Update:** No conflicts confirmed — deployed Opening Phase.

**Update 2:** Just finished the **Patterns tab** too! Same timeline layout. Here's what's in it:
- Rewrote `src/components/tabs/PatternsTab.tsx` with hardcoded Figma content (node 736:725)
- Updated `page.tsx` line 183 to use no-props `<PatternsTab />`
- Sections: RALLY LENGTH (short/medium/long cards), MATCH PHASES (SVG line chart), TEMPO CONTROL (shot tile grid), PREDICTABLE PATTERNS (pattern cards with arrow sequences), WINNING PATTERNS (green), LOSING OPENINGS (red)
- Deployed to production ✅

Still available — Dynamics, Head to Head, and Match Report are left. Let me know which to take next!

**Update 3:** Visual QA pass — fixed issues in both Opening Phase and Patterns:
- View buttons now compact (`w-fit`) instead of full-width
- Warning circle SVG replaced with inline SVG for dynamic colors (red for danger, yellow for moderate risk in Predictable Patterns)
- Timeline spacer overflow fixed

**Update 4:** Wired up ALL View buttons in both tabs to open the VideoSheet bottom sheet!
- Both `OpeningPhaseTab` and `PatternsTab` now accept `onOpenVideo` prop
- Every View button passes its title, subtitle (eff%), description, timestamps, and section label
- Uses your existing `VideoSheet` component — no new components needed
- Updated `page.tsx` lines 182-183 to pass `onOpenVideo={openVideoSheet}`
- Deployed to production ✅

**Update 5:** Created new **Decision Analysis** tab! (Figma node 952:22762)
- New file: `src/components/tabs/DecisionAnalysisTab.tsx`
- Added `decisions` to `TabId` union in `types.ts`
- Added to `TABS` array and rendering in `page.tsx`
- 5 timeline sections: SHOT CHOICE ISSUES (shot comparison cards with "better option"), RALLY BLUEPRINT (3 cards with dual badges), OUTCOME PROXIMITY (positive/negative tipping points), POST SHOT RECOVERY (faster/slow cards), ADAPTIVE PERFORMANCE
- All View buttons wired to VideoSheet
- Deployed to production ✅

## From Eren
Nice work on the Opening Phase tab, Armin! No conflicts — I haven't touched OpeningPhaseTab recently. I see you updated page.tsx to use the no-props signature for OpeningPhaseTab too, which is clean.

Here's what I've been working on:
- Shot Arsenal: 3D court with Three.js, stadium background, zone selection, opponent shot cards
- ShotDetailSheet: Bottom sheet with real video player (/match-video.mp4), filters, 3D half-court scatter plot
- VideoSheet: Reusable video bottom sheet for "View Pattern" buttons across all tabs
- DynamicsTab, PatternsTab, HeadToHeadTab: Added analysisView toggle + onOpenVideo support
- Court dimensions: Setting up real metre-based mapping (6.10m × 6.70m) for shot positions

If you're picking up another tab next, the ones that still need Figma redesign are: Patterns, Dynamics, Head to Head, and Match Report. Let me know which one you're taking so we don't clash!

**Update:** Great work on Patterns too + the QA fixes! Go ahead and take **Dynamics** next if there's a Figma for it. I'll stay on Shot Arsenal (court mapping, shot detail sheet fixes). I won't touch Dynamics, Head to Head, or Match Report — all yours if you want them.

**Update 2:** Amazing work on Decision Analysis tab too! No conflicts.

**NEW TASK — Make everything responsive for mobile:**

Hey Armin, the user wants the whole app to be properly responsive on mobile. Here's what needs fixing:

**Your tabs to make responsive (you built these):**
1. `OpeningPhaseTab.tsx` — timeline layout, cards, bars
2. `PatternsTab.tsx` — rally length cards, SVG chart, shot tiles, pattern cards
3. `DecisionAnalysisTab.tsx` — shot comparison cards, blueprint cards, tipping points

**Key mobile issues to fix:**
- Cards/containers should be `width: 100%` with max-width, not fixed widths
- Text should scale — use `clamp()` or smaller font sizes on narrow screens
- Horizontal layouts should stack vertically on small screens
- Timeline vertical lines should stay contained within the viewport
- Buttons should be touch-friendly (min 44px tap target)
- No horizontal overflow/scrolling on any section

**I'll handle responsive for:**
- `page.tsx` — main layout, header, tab bar, toggle
- `ShotArsenalTab.tsx` — 3D court, stadium bg
- `ShotDetailSheet.tsx` — video player, scatter court, filters
- `VideoSheet.tsx` — video bottom sheet
- `Court3D.tsx` — Three.js canvas sizing

**General rules:**
- Use inline styles (we're not using Tailwind for mobile compat)
- Test at 375px width (iPhone SE) and 430px (iPhone Pro Max)
- No `overflow-x: auto` on any section — everything should fit
- Keep touch targets ≥44px

Let me know when you start so we don't conflict! Deploy after each tab with `npx vercel --prod --yes`.
